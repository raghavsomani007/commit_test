
from setuptools import setup, find_packages
setup(
    name = 'test',
    version = '0.0.1',
    packages = ['test'],
    package_dir = {'test': '.'},
    description = '',
    install_requires = [],
)

